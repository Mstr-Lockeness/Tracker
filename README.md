This is a small self made program that i will be making for my wife and I, primarily as project structure practice. It will mainly be used for mount/reputation tracking, might add outfit collections at a later point.

As i further develope this, I will try to update this with all available features added. The following features are currently available for my wow tracker application:
    - A functioning stats page with broken down stats per expansion
    - A mount index with specialized filters 
    - A search bar that searches words on any 'key', works more like ctrl+F
    - An obtained checkbox that controls mount card background color for a visual indicator (a light pink if its unobtained, and a light green for being obtained)
    - A favorite checkbox that enables its own special, controllable filter
    - A very WIP moount list that i am still adding onto when given time
    
The following list below are planned features that i either have full intention of adding or wish to add but still need to learn the how, some easy some hard. The list is as followed:
    - A more narrowed search bar, excluding certain items on a card while holding others
    - A button on top of the page to send the anyone to an unfiltered mount list that displays everything (just something as a 'i just want this' thing)
    - An added stat to all mounts that allows its cross-faction counterpart to be checked at the same time, since it is technicalled owned now
    - Replace the 'stats' hyperlink at the top with a button that functionally does the same thing
    - Add/make new pages for reputations and transmogs, buttons similar to above included
    - Add a 'favorites panel' to the stats page that pulls a list of mounts you have favorited and displays a minimalized info card about it. Something along the lines of [Name] [Zone] [Drop] for format
    - Replace the hyperlinks on the side of the pages with a different format with button dropdown layouts and such (sorry if wording is bad)
    - Add aesthic changes to the page that makes it look more appealing... Eventually
